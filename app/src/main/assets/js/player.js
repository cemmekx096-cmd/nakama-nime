// assets/js/player.js
const Player = {
    play(url, options = {}) { Log.d('Player', `play ${url} — stub`); },
    pause() { Log.d('Player', 'pause — stub'); },
    stop() { Log.d('Player', 'stop — stub'); }
};