// assets/js/extension-manager.js
const ExtensionManager = {
    async loadAll() { Log.d('ExtensionManager', 'loadAll — stub'); },
    async install(pkg) { Log.d('ExtensionManager', `install ${pkg} — stub`); },
    async uninstall(pkg) { Log.d('ExtensionManager', `uninstall ${pkg} — stub`); }
};